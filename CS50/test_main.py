from main import *

# def test_split_or_treat() -> None:
    # assert split_or_treat(1,  184) == "Each person pays: $46.00"
    # assert split_or_treat(2, 180) == f"\n{random_person} pays the total bill of ${total_bill:.2f}.\nBetter luck next time"